<div class="header">
  <div class="container">
      <div class="row">
          <div class="col-xs-12">
            <h1 class='text-center Sports-Vilage' style='color:#FFFFFF'>Sports Vilage</h1>
          </div>
      </div>
  </div>
</div>

<% if logged_in? %>
  <%= render 'layouts/navbar_login' %>
<% else %>
  <%= render 'layouts/navbar' %>
<% end %>